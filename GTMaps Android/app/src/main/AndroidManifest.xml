<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.gatech.gtmaps">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.RoomChooseActivity"></activity>
        <activity android:name=".activities.DirectionsActivity"></activity>
        <activity android:name=".activities.SeeGalleryActivity"></activity>
        <activity android:name=".activities.RoomListActivity"></activity>
    </application>

</manifest>
